# BMStack Pages

A small React + Vite test website configured for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

## Deploy

Push the `main` branch to GitHub. In the repository settings, open **Pages** and set
the source to **GitHub Actions**. The included workflow builds and deploys the site.
